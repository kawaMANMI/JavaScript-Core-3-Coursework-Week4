let add = require("./add");

test("Sum of the numbers in a string", function () {
  // Arrange
  const numbers1 = "";
  output1 = add(numbers1);
  expect(output1).toEqual(1);

  // Act
  // Assert
});
